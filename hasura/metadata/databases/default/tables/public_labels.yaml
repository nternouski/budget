table:
  schema: public
  name: labels
array_relationships:
  - name: transaction_labels
    using:
      foreign_key_constraint_on:
        column: labelId
        table:
          schema: public
          name: transaction_labels
insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
        - id
        - createdAt
        - name
        - color
select_permissions:
  - role: public
    permission:
      columns:
        - color
        - name
        - createdAt
        - id
      filter: {}
delete_permissions:
  - role: public
    permission:
      backend_only: false
      filter: {}
